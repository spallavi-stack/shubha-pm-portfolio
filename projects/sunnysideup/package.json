{
  "name": "sunnysideup-calculator",
  "private": true,
  "description": "Unit tests for SunnySideUp's viability calculator (calculator.js).",
  "scripts": {
    "test": "node --test \"test/**/*.test.js\"",
    "test:coverage": "c8 --reporter=text --reporter=html node --test \"test/**/*.test.js\""
  },
  "devDependencies": {
    "c8": "^10.1.2"
  }
}
